<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hayao.ticketter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".EventAddActivity" />
        <activity android:name=".DefaultActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EventListActivity" />
        <activity android:name=".UserAddActivity" />
        <activity android:name=".UserListActivity" />
        <activity android:name=".TicketAddActivity" />
        <activity android:name=".TicketListActivity" />
        <activity android:name=".TicketSumActivity"></activity>
    </application>

</manifest>